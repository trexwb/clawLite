# 2026-09-24

## 编写 AGENTS.md 与版本说明（参考 fastenerTradeWorkbench）

- 新增 `AGENTS.md`：参考 `/Users/wbtrex/.../fastenerTradeWorkbench/AGENTS.md` 的强制规范结构，**但按 clawLite 真实架构适配**（Electron 桌面宿主，非 file:// SPA）：版本单一来源=package.json、IPC 三层对齐契约、严格 CSP、禁止移除 `--expose-internals`/`ELECTRON_RUN_AS_NODE` spawn 细节、内置 DSH 运行时版本独立于应用版本（fetch-runtime.mjs 的 DSH_VERSION）。
- 新增 `docs/version/README.md` 与 `docs/version/RELEASE-v1.0.md`：沿用 fTW 版本纪律（末位+1 唯一场景、禁止推进清单、不推进也要写日志）。
- 用户确认将 `package.json` version 由 `0.1.0` 同步为 `1.0.0` 作为当前基线（未发布前不递增）。
- 验证：`npm run check` 全绿（26 项 PASS，含 IPC 三层对齐 12 通道 / 2 事件 / 12 方法、无残留 Tauri 依赖）。
